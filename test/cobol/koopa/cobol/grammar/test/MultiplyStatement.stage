target multiplyStatement;

+[ MULTIPLY A BY B GIVING C ]
+[ MULTIPLY A BY B GIVING C D ]

+[ MULTIPLY A BY B GIVING C ROUNDED ]
+[ MULTIPLY A BY B GIVING C ROUNDED D ROUNDED ]

# Issue #28; adding extra checks for places where functions, object refs and
# reference mods are not allowed
-[ MULTIPLY A BY B GIVING FUNCTION FN ( X ) ]
-[ MULTIPLY A BY B GIVING EXCEPTION-OBJECT ]
-[ MULTIPLY A BY B GIVING NULL ]
-[ MULTIPLY A BY B GIVING SELF ]
-[ MULTIPLY A BY B GIVING SUPER ]
-[ MULTIPLY A BY B GIVING MY-CLASS-NAME OF SUPER ]
-[ MULTIPLY A BY B GIVING ADDRESS OF SOMETHING ]


+[ MULTIPLY A BY B ]
+[ MULTIPLY A BY B C ]

+[ MULTIPLY A BY B ROUNDED ]
+[ MULTIPLY A BY B ROUNDED C ROUNDED ]

# Issue #28; adding extra checks for places where functions, object refs and
# reference mods are not allowed
-[ MULTIPLY A BY FUNCTION FN ( X ) ]
-[ MULTIPLY A BY EXCEPTION-OBJECT ]
-[ MULTIPLY A BY NULL ]
-[ MULTIPLY A BY SELF ]
-[ MULTIPLY A BY SUPER ]
-[ MULTIPLY A BY MY-CLASS-NAME OF SUPER ]
-[ MULTIPLY A BY ADDRESS OF SOMETHING ]


+[ MULTIPLY A BY B ] <> [ . ]

+[ MULTIPLY A BY B GIVING C ] <> [ . ]

+[ MULTIPLY A BY B
     ON SIZE ERROR
        DISPLAY "OOPS" ]

+[ MULTIPLY A BY B GIVING C
     ON SIZE ERROR
        DISPLAY "OOPS" ]

+[ MULTIPLY A BY B
     SIZE ERROR
        DISPLAY "OOPS" ]

+[ MULTIPLY A BY B GIVING C
     SIZE ERROR
        DISPLAY "OOPS" ]

+[ MULTIPLY A BY B
     NOT ON SIZE ERROR
        DISPLAY "AOK" ]

+[ MULTIPLY A BY B GIVING C
     NOT ON SIZE ERROR
        DISPLAY "AOK" ]

+[ MULTIPLY A BY B
     NOT SIZE ERROR
        DISPLAY "AOK" ]

+[ MULTIPLY A BY B GIVING C
     NOT SIZE ERROR
        DISPLAY "AOK" ]

+[ MULTIPLY A BY B
     ON SIZE ERROR
        DISPLAY "OOPS"
     NOT ON SIZE ERROR
        DISPLAY "AOK" ]

+[ MULTIPLY A BY B GIVING C
     ON SIZE ERROR
        DISPLAY "OOPS"
     NOT ON SIZE ERROR
        DISPLAY "AOK" ]

+[ MULTIPLY A BY B
     ON SIZE ERROR
        DISPLAY "OOPS"
     NOT ON SIZE ERROR
        DISPLAY "AOK"
   END-MULTIPLY ]

+[ MULTIPLY A BY B GIVING C
     ON SIZE ERROR
        DISPLAY "OOPS"
     NOT ON SIZE ERROR
        DISPLAY "AOK"
   END-MULTIPLY ]

+[ MULTIPLY A BY B
   END-MULTIPLY ]

+[ MULTIPLY A BY B GIVING C
   END-MULTIPLY ]
