package koopa.grammars.cobol.test;
grammar koopa.grammars.cobol.CobolGrammar;

target ifStatement;

+[ IF 1 < 2 THEN
     DISPLAY GOOD 
   END-IF ]

+[ IF 1 < 2
     DISPLAY GOOD
   END-IF ]

+[ IF 1 < 2 THEN
     DISPLAY GOOD ]

+[ IF 1 < 2
     DISPLAY GOOD ]

+[ IF 1 < 2 THEN
     DISPLAY 'GOOD'
     DISPLAY BETTER
     DISPLAY BEST
   END-IF ]

+[ IF 1 < 2
     DISPLAY GOOD
     DISPLAY BETTER
     DISPLAY BEST
   END-IF ]

+[ IF 1 < 2 THEN
     DISPLAY GOOD
     DISPLAY BETTER
     DISPLAY BEST ]

+[ IF 1 < 2 
     DISPLAY GOOD
     DISPLAY BETTER
     DISPLAY BEST ]

+[ IF 1 < 2 THEN
     DISPLAY GOOD
   ELSE
     DISPLAY BAD
   END-IF ]

+[ IF 1 < 2
     DISPLAY GOOD
   ELSE
     DISPLAY BAD
   END-IF ]

+[ IF 1 < 2 THEN
     DISPLAY GOOD
     DISPLAY BETTER
     DISPLAY BEST
   ELSE
     DISPLAY BAD
     DISPLAY WORSE
     DISPLAY WORST
   END-IF ]

+[ IF 1 < 2 THEN
     DISPLAY GOOD
     DISPLAY BETTER
     DISPLAY BEST
   ELSE
     DISPLAY BAD
     DISPLAY WORSE
     DISPLAY WORST ]

+[ IF 1 < 2 THEN
     DISPLAY GOOD
   ELSE
     DISPLAY BAD ]

+[ IF 1 < 2
     DISPLAY GOOD
   ELSE
     DISPLAY BAD ]

+[ IF 1 < 2 THEN
     DISPLAY GOOD
     DISPLAY BETTER
     DISPLAY BEST
   ELSE
     DISPLAY BAD
     DISPLAY WORSE
     DISPLAY WORST ]

+[ IF 1 < 2
     DISPLAY GOOD
     DISPLAY BETTER
     DISPLAY BEST
   ELSE
     DISPLAY BAD
     DISPLAY WORSE
     DISPLAY WORST ]

+[ IF 1 < 2
     DISPLAY GOOD
   ELSE
     NEXT SENTENCE
   END-IF ]

+[ IF 1 < 2
     NEXT SENTENCE
   ELSE
     DISPLAY BAD
   END-IF ]
   
 +[ IF 1 < 2 AND 2 > 1
 		DISPLAY GOOD
 	END-IF ]
 	
 +[ IF 1 < 2 AND 2 > 1 AND V-ARIABLE = 0
 		DISPLAY GOOD
 	END-IF ]

+[ IF V-ARIABLE = 2 OR = 3
 	DISPLAY PERFECT
 	END-IF ]
 	
 +[ IF ( V-ARIABLE = 2 OR = 3 ) THEN
 	DISPLAY PERFECT
 	END-IF ]
 	
 +[ IF ( V-ARIABLE = 2 OR = 3 ) AND V-ARIABLE NOT = 3
 	DISPLAY PERFECT
 	END-IF ]
 	
 +[ IF V-ARIABLE NOT = 3 AND ( V-ARIABLE = 2 OR = 3 )
 	DISPLAY PERFECT
 	END-IF ]
   